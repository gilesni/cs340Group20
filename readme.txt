Server was built using Node.js.
In order to get the necessary packages use the command "npm install".
To run the server use the command "node server.js portnumber", where portnumber is an unused port.
The server will not work without the necessary packages.
Lastly all of the SQL for this project is under the sql folder.
